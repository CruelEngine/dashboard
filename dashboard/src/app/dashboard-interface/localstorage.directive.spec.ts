import { LocalstorageDirective } from './localstorage.directive';

describe('LocalstorageDirective', () => {
  it('should create an instance', () => {
    const directive = new LocalstorageDirective();
    expect(directive).toBeTruthy();
  });
});
